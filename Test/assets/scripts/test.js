const A = [1, 5, 1, 5, 9, 7, 7, 8, 6, 8];

/* B = {};
B[5] = "yolo";
console.log(B);
console.log(B.test);
 */
c = undefined;
console.log(c);
++c;
console.log(c);
Object.keys(map).filter









/* A.forEach((val) => console.log(val));

let map = A.reduce((map, el) => {
  if (map[el] === undefined) {
    map[el] = 1;
  } else {
    map[el]++;
  }
  return map;
}, {});

function result(map) {
  for (properties in map) {
    if (map[properties] % 2 !== 0) {
      return parseInt(properties);
    }
  }
} */
//console.log(`Here is the result: ${result(map)}`);
